import Flow404 from '@/components/Flow404';

/* 404 ZNOTRAJ Flow aplikacije (/kalkulator/*) = Flow 404 (kužek + kabel),
   NE Pinart portfolio 404. */
export default function FlowNotFound() {
  return <Flow404 />;
}
